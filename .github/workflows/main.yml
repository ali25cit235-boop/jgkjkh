name: Rdp1-ByNTechDz

on:
  workflow_dispatch:

jobs:
  request-approval:
    runs-on: ubuntu-latest
    steps:
      - name: Send Waiting Alert to Discord
        run: |
          WEBHOOK_URL="https://discordapp.com/api/webhooks/1552653122311159839/k8LUVGUa5IawEggs_5I2r_oRDEatxXjwXeYSObx-nymSpC3ZJKGBvpBIAihClJr2gYVW"
          
          PAYLOAD='{
            "username": "RDP Manager Bot",
            "embeds": [
              {
                "title": "⏳ Wait for Admin Approval",
                "description": "New RDP request initiated. Please approve the workflow run in GitHub Actions to generate credentials.",
                "color": 16776960
              }
            ]
          }'
          
          curl -H "Content-Type: application/json" -X POST -d "$PAYLOAD" "$WEBHOOK_URL"
